// Archived Supabase client (JavaScript copy)
// Original TypeScript source was preserved elsewhere before conversion.
// This archived file is intentionally a non-executing placeholder to avoid
// triggering type-checker or runtime issues in the archive folder.

/*
import { createClient } from '@supabase/supabase-js';

const SUPABASE_URL = import.meta.env?.VITE_SUPABASE_URL || process.env.VITE_SUPABASE_URL;
const SUPABASE_PUBLISHABLE_KEY = import.meta.env?.VITE_SUPABASE_PUBLISHABLE_KEY || process.env.VITE_SUPABASE_PUBLISHABLE_KEY;

export const supabase = createClient(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: {
    storage: (typeof window !== 'undefined' && window.localStorage) || null,
    persistSession: true,
    autoRefreshToken: true,
  }
});
*/

export default null;
