// Runtime placeholder for Supabase types. The original TypeScript definitions were
// archived in `archive_backend/types.ts` for reference. This file prevents import
// errors at runtime if any modules import the types file.

export const Database = {};
